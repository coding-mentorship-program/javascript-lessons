/**
 * Comments in Javascript
 * - Single comment => 
 *    - while on any line of code =>  cmd + /
 * - multiline comment
 *    - while selecting multiple lines of code =>  shift + option + a
 */

function myFullName() {
	// 1. First get the number from the button
	// 2. then add that number to a function
	// 3. from that function you want to do the calculation
	console.log(' Im running outside of the browser 😎')
}
